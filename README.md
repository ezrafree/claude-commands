# Claude Commands

## Install

Clone the project locally

```sh
git clone https://github.com/ezrafree/claude-commands.git ~/.config/claude-commands/
```

Symlink to `~/.claude/commands/`

```sh
ln -s ~/.config/claude-commands/commands ~/.claude/commands
```

## Update

Whenever there's updates here you can update your commands with `git pull`

```sh
cd ~/.config/claude-commands && git pull
```
